@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
<ul class="nav navbar-nav navbar-right">
    <li>
        <span class="artur-link">Добрый день! @User.Identity.GetUserName()</span>
    </li>
    <li>@Html.ActionLink("Выйти", "Logout", "Account", routeValues: null, htmlAttributes: new { id = "logOffLink", @class = "artur-link" })</li>
</ul>
    }

else
{
    <ul class="nav navbar-nav navbar-right">
        @*<li >@Html.ActionLink("Регистрация", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "artur-link" })</li>*@
        <li >@Html.ActionLink("Выполнить вход", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "artur-link" })</li>
    </ul>
}

